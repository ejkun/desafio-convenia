<?php

$finder = PhpCsFixer\Finder::create()
    ->exclude('storage')
    ->exclude('vendor')
    ->exclude('node_modules')
    ->in(__DIR__)
;

return PhpCsFixer\Config::create()
    ->setRules([
        '@PhpCsFixer' => true,
    ])
    ->setFinder($finder)
;
